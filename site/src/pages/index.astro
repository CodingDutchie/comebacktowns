---
import Base from "../layouts/Base.astro";
import { site } from "../lib/site";
---

<Base>
  <h1>{site.SITE_NAME}</h1>
  <p>{site.TAGLINE}</p>
  <p>
    Readiness grades for about 150 cities and villages in the Hudson Valley, Capital Region and
    Mohawk Valley are being built. Town pages, rankings and the methodology arrive with the
    data pipeline.
  </p>
</Base>
